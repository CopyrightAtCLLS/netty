syntax = "proto3";//协议版本
option java_outer_classname = "StudentPOJO";//生成的外部类名
//protobuf使用message管理数据
//会在外部类StudentPOJO生成一个内部类Student，它是真正发送的POJO对象
message Student{
  int32 id = 1;//Student类中有一个属性名字为id类型为int32。1表示属性序号不是值!!!
  string name = 2;
}

